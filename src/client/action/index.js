import * as searchActions from './search_actions';
import * as searchActionsTypes from './search_actions_types';
import searchActionsPropTypes from './search_actions_prop_types';
import * as wishlistActions from './wishlist_actions';
import * as wishlistActionsTypes from './wishlist_actions_types';
import wishlistActionsPropTypes from './wishlist_actions_prop_types';

export {
  searchActions,
  searchActionsTypes,
  searchActionsPropTypes,
  wishlistActions,
  wishlistActionsTypes,
  wishlistActionsPropTypes
};

export default {
  searchActions,
  searchActionsTypes,
  searchActionsPropTypes,
  wishlistActions,
  wishlistActionsTypes,
  wishlistActionsPropTypes
};
