/**
 * CSRF Token key.
 *
 * Used by client and server sides to keep csrf token in cookie which
 * can be accessed by using of this defined key.
 */
export const csrfTokenKey = 'x-csrf-token';

export default csrfTokenKey;
